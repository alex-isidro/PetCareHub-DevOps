services:

  petcarehub:
    build: .

    image: challenge-clyvo:v1

    container_name: petcarehub

    ports:
      - "8080:8080"

    environment:
      DB_URL: "jdbc:oracle:thin:@oracle.fiap.com.br:1521/orcl"
      DB_USERNAME: "rm565554"
      DB_PASSWORD: "230806"

    restart: unless-stopped